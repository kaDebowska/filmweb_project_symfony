{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.profile_settings'|trans}}
{% endblock %}

{% block body %}
    <h1>{{ 'title.profile_settings'|trans}}</h1>
    <a href="{{ url('user_change_password', {id: user.id}) }}" title="{{ 'action.change_password'|trans }}" class="btn btn-primary btn-lg">
        {{ 'action.change_password' | trans }}
    </a>
{% endblock %}